# Used automatically by `npm run build`. Same-origin API path because the
# FastAPI backend serves this built app (single deployable service).
# For a SEPARATE frontend host, set this to your backend URL instead, e.g.
#   VITE_API_BASE=https://your-backend.onrender.com/api/v1
VITE_API_BASE=/api/v1
