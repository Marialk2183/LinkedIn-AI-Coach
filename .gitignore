# Python
__pycache__/
*.py[cod]
.pytest_cache/
backend/.venv/
backend/*.db
backend/.env

# ML artifacts (reproducible: `python -m ml.train`)
backend/ml/model.pkl

# Node / frontend
node_modules/
# NOTE: frontend/dist/ is committed on purpose — the Python deploy host has no
# Node, so it serves this prebuilt bundle. Rebuild + recommit after frontend edits.
frontend/.env
frontend/.tsnode/
*.tsbuildinfo
*.local

# Editor / OS
.vscode/*
.idea
.DS_Store
Thumbs.db

# Logs
*.log
